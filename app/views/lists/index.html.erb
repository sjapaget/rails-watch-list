<h1>All Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
    <%= list.name %>
    <%= link_to "See List", list_path(list) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a list", new_list_path %>
